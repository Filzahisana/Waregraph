import 'package:flutter/material.dart';

class PipaBesar extends StatelessWidget {
  const PipaBesar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pipa Besar'),
    );
  }
}
